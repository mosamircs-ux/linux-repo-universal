# AetherOS zRAM Generator Configuration
# /etc/systemd/zram-generator.conf

[zram0]
# Allocate 50% of total physical RAM up to 8GB as compressed zRAM swap device
zram-size = min(ram / 2, 8192)
compression-algorithm = zstd
swap-priority = 100
fs-type = swap
