# ==============================================================================
# AetherOS Default Firewall (UFW) Configuration
# Path: /etc/ufw/ufw.conf
# ==============================================================================

# Enable firewall by default on boot
ENABLED=yes

# Log level (low, medium, high, full)
LOGLEVEL=low
